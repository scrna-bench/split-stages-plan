{
  "stage": "EMBED-M",
  "version": "0.1.2",
  "benchmark": "omni-scrna/split-stages-plan",
  "args": [
    { "flag": "--embedding_tsv", "type": "path", "help": "Embedding TSV (cell_ids as rownames)" },
    { "flag": "--rawdata_clusters_truth", "type": "path", "help": "TSV of ground-truth cluster labels (cell_id, truths)" }
  ]
}
